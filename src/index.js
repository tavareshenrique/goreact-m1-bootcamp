import React from 'react';
import { render } from 'react-dom';

export default function App() {
  return (
    <div>
      <h1>My Pure Base to React</h1>
    </div>
  );
}

render(<App />, document.getElementById('app'));
